import 'package:flutter/material.dart';
import 'package:lab4_multipage/components/formpage.dart';

// function to trigger build when the app is run
void main() {
  runApp(const MaterialApp(
    title: 'Form Page',
    home: FormPage(),
  )); //MaterialApp
}
